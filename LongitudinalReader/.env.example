# The Longitudinal Reader reads the Anthropic key from (in order):
#   1. an ANTHROPIC_API_KEY already in the environment
#   2. a local LongitudinalReader/.env (this file, renamed)
#   3. SecondReader/poc/.env  (the shared hackathon key)
ANTHROPIC_API_KEY=sk-ant-...
